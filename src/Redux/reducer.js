const initialState = {
    currentUser: {},
    loggedIn: false,
    currentTrip: {}
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default reducer; 